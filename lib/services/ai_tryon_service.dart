import 'dart:async';

class AiTryOnService {
  Future<String> generateTryOn({required String userPhotoUrl, required String garmentImageUrl, String? prompt}) async {
    // Placeholder: integrate with your chosen model endpoint.
    await Future.delayed(const Duration(seconds: 2));
    return 'https://placehold.co/1024x1365/png';
  }
}
